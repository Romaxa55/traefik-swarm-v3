version: "3.9"

services:
  minio1:
    image: minio/minio:RELEASE.2024-06-13T22-53-53Z



volumes:
  minio1-data:
    driver: local
  minio2-data:
    driver: local
  minio3-data:
    driver: local

networks:
  minio_distributed:
    driver: overlay

secrets:
  secret_key:
    external: true
  access_key:
    external: true
